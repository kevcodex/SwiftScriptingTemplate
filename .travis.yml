matrix:
  include:
    - os: 
      - linux
      language: generic
      install: swift package update
      script:
        - swift build
        - swift test
        
    - os:
      - osx
      language: swift
      osx_image: xcode9.4
      install: swift package update
      script:
        - swift build
        - swift test

